#include "main.h"
/**
 * main - printing out alphabets in lowercase 
 * by thenoskshade
 * Return: 0
*/
void print_alphabet(void)
{
	char nosk;

	for (nosk = 'a'; nosk <= 'z'; nosk++)
	{
		_putchar(nosk);
	}
	-putchar('\n');
}
