#include "main.h"
/**
 * add - function to add two integers and returns the result
 * noskshade(dreamwalking)
 * @n: int to be added to d
 * @d: int to be added to n
 * Return: sum of n and b
*/
int add(int n, int d)
{
	int f;

	f = n + d;

	return (f);
}
